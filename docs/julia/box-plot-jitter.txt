using Plotly
Plotly.signin({% if username %}"{{username}}"{% else %}"JuliaAPI"{% endif %}, {% if api_key %}"{{api_key}}"{% else %}"3bule2whyg"{% endif %})

data = [
  [
    "y" => [0, 1, 1, 2, 3, 5, 8, 13, 21], 
    "type" => "box", 
    "boxpoints" => "all", 
    "jitter" => 0.3, 
    "pointpos" => -1.8
  ]
]

response = Plotly.plot([data], ["filename" => "box-plot-jitter", "fileopt" => "overwrite"])
plot_url = response["url"]