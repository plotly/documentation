library(plotly)

p &lt;- plotly(username={% if username %}"{{username}}"{% else %}'rAPI'{% endif %}, key={% if api_key %}"{{api_key}}"{% else %}'yu680v5eii'{% endif %})

data &lt;- list(
  list(
    x = c(0, 2, 4), 
    y = c(0, 4, 2), 
    type = "scatter"
  )
)

response &lt;- p$plotly(data, kwargs=list(filename="privacy-true", fileopt="overwrite"))
url &lt;- response$url
filename &lt;- response$filename