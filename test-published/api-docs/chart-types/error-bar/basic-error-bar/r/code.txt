library(plotly)
p &lt;- plotly(username='TestBot', key='r1neazxo9w')
data &lt;- list(
  list(
    x = c(0, 1, 2), 
    y = c(6, 10, 2), 
    error_y = list(
      type = "data", 
      array = c(1, 2, 3), 
      visible = TRUE
    ), 
    type = "scatter"
  )
)
response &lt;- p$plotly(data, kwargs=list(auto_open=FALSE, fileopt="overwrite", filename="basic-error-bar"))
url &lt;- response$url