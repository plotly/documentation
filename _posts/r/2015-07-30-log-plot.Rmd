---
title: Log Plots in R | Examples | Plotly
name: Log Plots
permalink: r/log-plot/
description: How to make a plot with logarithmic axes in R.
layout: base
thumbnail: thumbnail/log.jpg
language: r
page_type: example_index
has_thumbnail: false
display_as: chart_type
order: 10
output: 
  html_document:
    keep_md: true
---

```{r, echo = FALSE}
knitr::opts_chunk$set(message = FALSE, results = 'hide')
Sys.setenv("plotly_username"="RPlotBot")
Sys.setenv("plotly_api_key"="q0lz6r5efr")
```
# Log Axes in R

```{r}
library(plotly)
d <- diamonds[sample(nrow(diamonds), 1000), ]
# without log scales
p <- plot_ly(d, x = ~carat, y = ~price) %>% add_markers()
p
```

```{r, echo=FALSE}
plotly_POST(filename="log/no-log")
```

```{r}
# with log scales
layout(p, xaxis = list(type = "log"),
       yaxis = list(type = "log"))
```

```{r, echo=FALSE}
plotly_POST(filename="log/log")
```
