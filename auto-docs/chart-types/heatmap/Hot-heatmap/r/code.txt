library(plotly)

p &lt;- plotly(username={% if username %}"{{username}}"{% else %}'rAPI'{% endif %}, key={% if api_key %}"{{api_key}}"{% else %}'yu680v5eii'{% endif %})

size &lt;- 50
z &lt;- matrix(0, size, size)
for(r in 1:size) {
    for(c in 1:size) {
        z[r, c] &lt;- sqrt(r*c/size^2)
    }
}
data &lt;- list(
  list(
    z = z, 
    scl = "Hot", 
    type = "heatmap"
  )
)
layout &lt;- list(title = "Hot")

response &lt;- p$plotly(data, kwargs=list(layout=layout, filename="Hot-heatmap", fileopt="overwrite"))
url &lt;- response$url
filename &lt;- response$filename