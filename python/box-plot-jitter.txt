import plotly
py = plotly.plotly('PythonAPI', 'ubpiol2cve')

from numpy import *
box = {'y': random.randn(50),
    'type': 'box',
    'boxpoints': 'all',
    'jitter': 0.3,
    'pointpos': -1.8}
py.plot([box])
